#ifndef ELIMINACION_H //verifica si el archivo esta definido 
#define ELIMINACION_H //si no esta definido, lo define 

void eliminarRegistro(int idEliminar);// Declaración de funcion que recibe una id y elimina el registro de la misma id

#endif //termina con la verficación para inclusion del archivo
#pragma once// Directiva para evitar la inclusión múltiple del archivo.
